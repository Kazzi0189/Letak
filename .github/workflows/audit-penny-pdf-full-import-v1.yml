name: Audit Penny PDF full import v1

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Check JavaScript syntax
        run: node --check scripts/audit-penny-pdf-full-import-v1.mjs

      - name: Audit Penny PDF full import v1
        run: node scripts/audit-penny-pdf-full-import-v1.mjs

      - name: Commit audit report
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add scripts/audit-penny-pdf-full-import-v1.mjs data/penny-probe/penny-pdf-full-import-v1-summary.json

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Audit Penny PDF full import v1"
            git push
          fi
