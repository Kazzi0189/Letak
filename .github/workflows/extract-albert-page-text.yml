name: Extract Albert page text

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Extract Albert hidden page text
        run: node scripts/extract-albert-page-text.mjs

      - name: Commit Albert hidden page text
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add data/albert-probe/page-text-summary.json data/albert-probe/albert-page-text-debug.json

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Extract Albert hidden page text"
            git push
          fi
