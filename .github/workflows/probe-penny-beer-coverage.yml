name: Probe Penny beer coverage

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Probe Penny beer coverage
        run: node scripts/probe-penny-beer-coverage.mjs

      - name: Check JavaScript syntax
        run: node --check scripts/probe-penny-beer-coverage.mjs

      - name: Commit Penny beer coverage report
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add scripts/probe-penny-beer-coverage.mjs data/penny-probe/penny-beer-coverage-summary.json

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Probe Penny beer coverage"
            git push
          fi
